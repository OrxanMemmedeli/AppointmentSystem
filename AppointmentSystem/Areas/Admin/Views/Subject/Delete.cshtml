@model SubjectViewModel
@{
    ViewData["Title"] = "Fənn Silinməsi";
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card shadow-sm border-danger">
                <div class="card-header bg-danger text-white">
                    <h4 class="mb-0">
                        <i class="bi bi-exclamation-triangle"></i> Fənn Silinməsi Təsdiqi
                    </h4>
                </div>
                <div class="card-body">
                    <div class="alert alert-danger">
                        <h5 class="alert-heading">
                            <i class="bi bi-shield-exclamation"></i> Xəbərdarlıq!
                        </h5>
                        <p class="mb-0">
                            Bu fənni silmək istədiyinizdən əminsiniz? Bu əməliyyat geri qaytarıla bilməz
                            və fənnə aid bütün əlaqələr (müəllimlər, şirkətlər) əlçatan olmayacaq.
                        </p>
                    </div>

                    <!-- Fənn Məlumatları -->
                    <div class="card bg-light mb-4">
                        <div class="card-body">
                            <h5 class="card-title mb-3">
                                <i class="bi bi-book text-primary"></i>
                                <strong>@Model.Name</strong>
                            </h5>

                            <div class="row mb-2">
                                <div class="col-4 text-muted">Kod:</div>
                                <div class="col-8">
                                    @if (!string.IsNullOrEmpty(Model.Code))
                                    {
                                        <code>@Model.Code</code>
                                    }
                                    else
                                    {
                                        <span class="text-muted">-</span>
                                    }
                                </div>
                            </div>

                            @if (!string.IsNullOrEmpty(Model.Description))
                            {
                                <div class="row mb-2">
                                    <div class="col-4 text-muted">Təsvir:</div>
                                    <div class="col-8">@Model.Description</div>
                                </div>
                            }

                            <div class="row">
                                <div class="col-4 text-muted">Status:</div>
                                <div class="col-8">
                                    <span class="badge bg-@(Model.IsActive ? "success" : "danger")">
                                        @(Model.IsActive ? "Aktiv" : "Deaktiv")
                                    </span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Təsir Zonası -->
                    <div class="alert alert-warning">
                        <h6><i class="bi bi-info-circle"></i> Bu əməliyyatın nəticələri:</h6>
                        <ul class="mb-0 small">
                            <li>Fənn sistemdən silinəcək (soft delete)</li>
                            <li>Fənnə aid olan <strong>bütün müəllim əlaqələri</strong> əlçatan olmayacaq</li>
                            <li>Fənnə aid olan <strong>bütün şirkət əlaqələri</strong> əlçatan olmayacaq</li>
                            <li>Müəllimlər bu fənni tədris edə bilməyəcək</li>
                            <li>Şirkətlər bu fənni istifadə edə bilməyəcək</li>
                            <li>Bu əməliyyat yalnız admin müdaxiləsi ilə geri qaytarıla bilər</li>
                        </ul>
                    </div>

                    <form asp-action="Delete" method="post">
                        @Html.AntiForgeryToken()
                        <input type="hidden" asp-for="Id" />

                        <div class="d-flex justify-content-between gap-2">
                            <a asp-action="Index" class="btn btn-secondary btn-lg flex-fill">
                                <i class="bi bi-x-circle"></i> Ləğv Et
                            </a>
                            <button type="submit" class="btn btn-danger btn-lg flex-fill">
                                <i class="bi bi-trash"></i> Bəli, Sil
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            <!-- Alternativ Əməliyyatlar -->
            <div class="card mt-3 border-primary">
                <div class="card-body">
                    <h6 class="text-primary mb-3">
                        <i class="bi bi-lightbulb"></i> Alternativ Həllər
                    </h6>
                    <div class="d-grid gap-2">
                        <form asp-action="ToggleStatus" asp-route-id="@Model.Id" method="post">
                            @Html.AntiForgeryToken()
                            <button type="submit" class="btn btn-outline-warning w-100">
                                <i class="bi bi-@(Model.IsActive ? "pause" : "play")-circle"></i>
                                Silmək əvəzinə @(Model.IsActive ? "Deaktiv" : "Aktiv") Et
                            </button>
                        </form>

                        <a asp-action="Edit" asp-route-id="@Model.Id"
                           class="btn btn-outline-primary">
                            <i class="bi bi-pencil"></i> Fənni Redaktə Et
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>